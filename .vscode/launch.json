{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Python: Attach to Docker",
            "type": "python",
            "request": "attach",
            "pathMappings": [
                {
                    "localRoot": "${workspaceFolder}",
                    "remoteRoot": "/app"  // Update this path as per your application location in Docker container
                }
            ],
            "port": 5678, 
            "host": "localhost"
        }
    ]
}
